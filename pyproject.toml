[tool.ruff]
line-length = 120     # wider than Ruff's 88 default
target-version = "py312"

[tool.ruff.lint]
# Ruff's defaults (E4, E7, E9, F) plus:
extend-select = [
    "I",   # isort
    "B",   # bugbear
    "UP",  # pyupgrade
]
